// Write a program that has one function with two arguments.
// If one of these arguments is greater than or equal to 18, the function returns true. Otherwise it returns false.
// The program calls the function with arguments 15, 4 and prints the function result.
 

function f(a,b){
   return (a>=18 || b>=18);
}

f(15,4)