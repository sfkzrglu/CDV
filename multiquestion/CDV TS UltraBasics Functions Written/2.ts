// Create a function that subtracts the second argument from the first and returns the result.
// The program should call this function with arguments (9, 5) and display the result.

// 4
 

function myFunction(val1:number,val2:number):number{
    return val1-val2;
}

let num:number=myFunction(9,5);
console.log(num);