// Create two variables with two numbers, and using a condition the program outputs the smallest one.
// Given code:
// The smallest number is 4.
 

let a=4;
let b=5;
if(a<b)
{
  console.log("The smallest number is "+a);
}else
{
  console.log("The smallest number is "+b);
}